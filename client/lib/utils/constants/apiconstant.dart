class Apiconstant {
  static const String baseUrl = 'http://localhost:3000/api';
  static const Duration connectTimeout = const Duration(seconds: 10);
  static const Duration receiveTimeout = const Duration(seconds: 10);
}